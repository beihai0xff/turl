version: '3.1'

services:

  mysql:
    image: mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: tiny-url
      MYSQL_ROOT_PASSWORD: test123
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]