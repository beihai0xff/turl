package tddl

// Config is the configuration for tddl
type Config struct {
	// Step is the step of the sequence
	Step uint64
	// SeqName is the name of the sequence
	SeqName string
	// StartNum is the start number of the sequence
	StartNum uint64
}
